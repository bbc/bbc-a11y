module.exports = {
  name: 'Focused elements must be identifiable',

  type: 'manual',

  failsForEach: 'focusable element that does not have a clearly identifiable visual style when it has focus',

  manualTest: {
    question: 'Do all focusable elements have a clearly identifiable visual style when they have focus?'
  }
}
